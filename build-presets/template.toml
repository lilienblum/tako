# Build preset template.
# Optional artifact exclusions:
# exclude = ["**/*.map"]
#
# Optional asset roots merged into app public/ after build:
# assets = ["dist/client", "assets/shared"]

[targets]
builder_image = "your-image:tag"
install = '''
# Install dependencies
'''
build = '''
# Build project output
'''

# Optional per-target overrides:
# [targets.linux-x86_64-glibc]
# builder_image = "your-image:tag"
#
# [targets.linux-aarch64-glibc]
# builder_image = "your-image:tag"
