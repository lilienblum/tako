[tools]
rust = "1.98"
bun = "1.4.0"
go = "1.27.0"
just = "1"
hk = "1.56.1"

[bootstrap.packages]
"brew:lychee" = "latest"

[hooks]
# Install/refresh git hooks whenever tools are installed
postinstall = "hk install --mise"
