# JavaScript Examples

JavaScript runtime examples for Tako.

## Available

- `bun/`: Bun app example using the `tako.sh` SDK wrapper.

## Run

From repository root:

```bash
just tako examples/js/bun dev
```

Or run directly from the example directory (without Tako proxy flow):

```bash
cd examples/js/bun
bun install
bun run dev
```
