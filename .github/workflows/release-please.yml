name: Release Please

on:
  push:
    branches: [master]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    environment: release
    outputs:
      prs: ${{ steps.release.outputs.prs }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          token: ${{ secrets.GH_PAT }}

  # release-please does string replacement on Cargo.lock which can leave it
  # out of sync. This job checks out each release PR branch, runs cargo update
  # to regenerate the lockfile, and pushes the fix if needed.
  fix-lockfile:
    name: Fix Cargo.lock
    needs: release-please
    if: needs.release-please.outputs.prs != '[]' && needs.release-please.outputs.prs != ''
    runs-on: ubuntu-latest
    environment: release
    strategy:
      fail-fast: false
      matrix:
        pr: ${{ fromJson(needs.release-please.outputs.prs) }}
    steps:
      # Skip if the release PR was merged (and branch deleted) between when
      # release-please-action computed its output and when this job runs.
      - name: Check branch still exists
        id: branch
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          if gh api "repos/${{ github.repository }}/branches/${{ matrix.pr.headBranchName }}" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "Branch ${{ matrix.pr.headBranchName }} no longer exists (PR likely merged). Skipping."
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout PR branch
        if: steps.branch.outputs.exists == 'true'
        uses: actions/checkout@v6
        with:
          ref: ${{ matrix.pr.headBranchName }}
          token: ${{ secrets.GH_PAT }}

      - name: Check for Cargo.toml changes
        id: check
        if: steps.branch.outputs.exists == 'true'
        run: |
          git fetch origin master --depth=1
          if git diff origin/master --name-only | grep -q 'Cargo.toml'; then
            echo "needs_fix=true" >> "$GITHUB_OUTPUT"
          else
            echo "No Cargo.toml changes, skipping"
          fi

      - name: Setup Rust
        if: steps.check.outputs.needs_fix == 'true'
        uses: dtolnay/rust-toolchain@stable

      - name: Update workspace lockfile
        if: steps.check.outputs.needs_fix == 'true'
        run: cargo update --workspace

      - name: Push if changed
        if: steps.check.outputs.needs_fix == 'true'
        run: |
          if git diff --quiet Cargo.lock; then
            echo "Cargo.lock is already correct"
          else
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add Cargo.lock
            git commit -m "chore: regenerate Cargo.lock"
            git push
          fi
